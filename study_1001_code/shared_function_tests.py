# Put shared functions tests here

# import unittest
# unit tests for load_otu_seqs:
#   # Convert below to a test for future imports to run!
# assert len(phyla_otu_id ) = len (seq...)
    #print len(phyla_OTU_IDs)
    #print len(sequences)
    #print set(phyla_OTU_IDs.keys()) == set(sequences.keys())


# test_taxa_split
# assert dataframe each column.count == column.count (no NANs!)

# test whether null values in sequence data are due to nulls/higher level taxa classes in
# otu testing
#print rep_seqs[pd.isnull(rep_seqs.phylum)]

